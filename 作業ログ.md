# DeepCast AI 作業ログ

## 2026-03-18 エンジン起動・全体監査・バグ修正

### セッション1: エンジン初期設定
- engine/ ディレクトリの全体構造を確認
- .env ファイル作成（Ollama, Gemini API, DB, Publisher設定）
- settings.py に GEMINI_API_KEY フィールド追加
- DB状態確認: published 1件, rejected 7件, pending topics 10件
- Ollama起動確認: qwen2.5モデル接続OK
- 依存ライブラリ: 全てインストール済み

### セッション2: セキュリティ・音声設定
- 全ソースコードからGemini APIキーのハードコードを削除（3ファイル）
- .gitignoreに.env, engine/data/, __pycache__追加
- Gemini API課金設定（従量課金、月上限3,000円）
- 新APIキー発行、旧キー無効化
- TTS確定: Gemini TTS（Enceladus=渋ホスト + Zephyr=お兄さんアシスタント）
- 対話形式、男性2人、女性ボイス全削除
- ffmpegで音量正規化 + ノイズ除去 + atempo=1.08

### セッション3: パイプライン・公開
- 100テーマリストからTier S/A 24トピックをDBにシード
- パイプライン実行: content #12〜#15 生成・published
- ep001.html, ep002.html サイト公開済み
- self_manager → Publisher自動連携修正
- voice生成 → DB metadata連携修正
- ダッシュボード起動（http://localhost:8001）

### セッション4: 全体コード監査・バグ修正
**5チームで並列監査 → 5チームで並列修正**

修正済み（CRITICAL）:
- voice_engine.py: ffmpeg concatパス → as_posix()（3箇所）
- voice_engine.py: タイムスタンプ → ミリ秒精度（2箇所）
- llm_client.py: response.json() に try-except追加（3箇所）
- brain.py: output_data Nullチェック追加（4箇所）
- sw.js: キャッシュ /js/main.js → /js/spa-router.js、v10→v11

修正済み（MEDIUM）:
- bridge.py: regex= → pattern=（4箇所、FastAPI deprecated対応）
- main.py: 全cmd関数にUTF-8 stdout設定（5関数）
- episodes.json: カテゴリ修正（中国語→tech、economy→business）
- episodes.json: 中国語タグ「道德」削除
- bridge.py: /api/generate のSelfManager初期化修正

### 現在のステータス
- pipeline: バックグラウンドで #15, #16 生成中
- voice: #11〜#14 の音声生成中（Gemini TTS対話モード）
- ダッシュボード: http://localhost:8001 稼働中

### 残タスク
- voice生成完了待ち → 音声ファイルをepisodes.jsonに紐づけ
- ダッシュボードサーバー再起動（bridge.pyの修正反映）
- git commit & push（engine/ + 新エピソード + 全修正）
- エピソードの記事品質改善（descriptionが途中切れ、口調がカジュアルすぎ）
- feed.xml enclosure length更新（実ファイルサイズ）
