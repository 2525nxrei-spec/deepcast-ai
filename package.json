{
  "name": "deepcast-ai",
  "version": "1.0.0",
  "description": "DeepCast AI — AIポッドキャスト配信サイト（Cloudflare Pages + Workers）",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.1",
    "vitest": "^3.1.1",
    "miniflare": "^4.20250317.0"
  }
}
